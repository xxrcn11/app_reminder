<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bt.sample.mapper.ReminderListMapper">

    <select id="findAll" resultType="ReminderListDto">
        SELECT id, name, color, icon, sort_order, created_at, updated_at
        FROM lists
        ORDER BY sort_order
    </select>

    <select id="findById" parameterType="long" resultType="ReminderListDto">
        SELECT id, name, color, icon, sort_order, created_at, updated_at
        FROM lists
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="ReminderListDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO lists (name, color, icon, sort_order)
        VALUES (#{name}, #{color}, #{icon}, #{sortOrder})
    </insert>

    <update id="update" parameterType="ReminderListDto">
        UPDATE lists
        SET name = #{name}, color = #{color}, icon = #{icon}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM lists
        WHERE id = #{id}
    </delete>

</mapper>
